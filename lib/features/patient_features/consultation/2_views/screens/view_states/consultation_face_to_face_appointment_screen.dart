import 'package:flutter/material.dart';

class FaceToFaceAppointmentScreen extends StatelessWidget {
  const FaceToFaceAppointmentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Face to face appointment'),
    );
  }
}
